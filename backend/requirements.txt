# ── Core Framework ──
fastapi==0.129.2
uvicorn==0.41.0
pydantic==2.12.5
python-dotenv==1.2.1
python-multipart==0.0.22

# ── Firebase (Auth + Firestore) ──
firebase-admin>=6.0.0

# ── Rate Limiting ──
slowapi>=0.1.9

# ── HTTP Client (News API) ──
httpx>=0.27.0

# ── AI Provider (Gemini) ──
google-genai>=1.0.0

# ── Testing ──
pytest>=9.0.0
